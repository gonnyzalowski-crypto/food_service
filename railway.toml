# Railway configuration
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
startCommand = "php setup_database.php && php -S 0.0.0.0:8000 -t web"
