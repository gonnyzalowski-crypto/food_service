# Railway configuration
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "php -S 0.0.0.0:$PORT -t web web/index.php"
healthcheckPath = "/health"
healthcheckTimeout = 120
